# Instalasi lokal selama ini memakai --legacy-peer-deps (plain install gagal
# ERESOLVE pada peer react-dom, lihat AGENTS.md). EAS cloud menjalankan
# npm install/ci polos, jadi konvensi ini dicatat di sini agar perilaku
# lokal dan cloud sama.
legacy-peer-deps=true
